{
  "accepted": [
    {
      "participants": [
        "http://localhost:8000",
        "http://localhost:8001",
        "http://localhost:8002",
        "http://localhost:8003"
      ],
      "program": ".example.garble.rs",
      "leader": 0,
      "party": 1,
      "input": "SELECT id, name, disabled FROM insurance",
      "db": "postgres://postgres:test@localhost:5551/postgres",
      "max_rows": 4
    }
  ]
}
